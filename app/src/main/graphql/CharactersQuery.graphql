query CharactersQuery($page: Int, $perPage: Int, $sort: [CharacterSort] = FAVOURITES_DESC) {
    Page(page: $page, perPage: $perPage) {
        characters(sort: $sort) {
            name {
                full
            }
            favourites
            gender
            image {
                large
            }
            id
            age
        }
    }
}